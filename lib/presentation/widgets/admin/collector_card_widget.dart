import 'package:flutter/material.dart';

// TODO: Create collector card widget for displaying collector information with status and actions
class CollectorCardWidget extends StatelessWidget {
  const CollectorCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
