import 'package:flutter/material.dart';

// TODO: Create navigation drawer for admin panel with menu items
class AdminDrawer extends StatelessWidget {
  const AdminDrawer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
